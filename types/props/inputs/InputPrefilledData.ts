export type InputPrefilledData = {
    placeholder: string,
    readOnly ?: boolean,
    labelText: string,
    inputMask?: string,
    numbersOnly?: boolean,
    endDecorator ?: string,
    disabled ?: boolean,
    isPassword ?: boolean,
    name : string,
}