export type InputPrefilledData = {
    placeholder: string,
    labelText: string,
    inputMask?: string,
    numbersOnly?: boolean,
    endDecorator ?: string,
    disabled ?: boolean,
    isPassword ?: boolean,
    name ?: string,
}